# Read enron.json and upload to elastic search
* Command line tool
* JSON Streaming
* Read json datasets from stream and upload this dataset to elastic search

## Usage
java -jar build.jar -input src/main/resources/enjon.json